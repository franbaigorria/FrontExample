//Reducer de los anuncios de juegos
const initialState = {
  newGames: null, //[{}]
};

export const newsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
